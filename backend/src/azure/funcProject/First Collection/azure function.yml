info:
  name: azure function
  type: http

http:
  method: POST
  url: http://localhost:7071/api/helloUser
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
